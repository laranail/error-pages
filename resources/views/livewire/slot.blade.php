{{-- The bare Livewire error component, for embedding inside a consumer's own
     component layout (`error-pages.livewire.layout`). The layout supplies the
     document chrome + @livewireStyles/@livewireScripts. --}}
<livewire:laranail-error-page :page="$page" />
