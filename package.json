{
  "name": "@laranail/server-error-pages",
  "private": true,
  "description": "Dev-only asset build for laranail/server-error-pages (Vite + Tailwind 4 + SCSS). Consumers never run this — the committed public/assets bundle is shipped.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:pretty": "vite build --mode pretty && npm run format:dist",
    "dev": "vite build --watch",
    "format": "prettier --write \"resources/assets/**/*.{scss,js}\"",
    "format:dist": "prettier --write \"public/assets/**/*.{css,js}\"",
    "format:check": "prettier --check \"resources/assets/**/*.{scss,js}\""
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "prettier": "^3.3.0",
    "sass": "^1.77.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0"
  }
}
